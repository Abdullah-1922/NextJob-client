/* eslint-disable react/prop-types */

const JobCart = ({job}) => {
    console.log(job);
    return (
        <div>
            <div>

            </div>
        </div>
    );
};

export default JobCart;